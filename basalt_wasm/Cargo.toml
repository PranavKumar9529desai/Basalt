[package]
name = "basalt_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
basalt_core = { path = "../basalt_core" }
basalt_fs = { path = "../basalt_fs" }
wasm-bindgen = "0.2"
