//! Wikilink target extraction — re-exported from the canonical implementation
//! in `basalt-types` (both crates need it; the leaf crate owns it per
//! CONVENTIONS §12.8).

pub use basalt_types::wikilink_target;
